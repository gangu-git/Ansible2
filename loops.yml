---
  - hosts: all
    become: yes
    tasks: 
      - name: practiceing Standard loops in ansible
        apt:
          name: "{{ item }}"
          state: present
          update_cache: yes
        loop: 
         - git
         - tree
         - apache2