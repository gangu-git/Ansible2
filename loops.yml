---
  - hosts: all
    become: yes
    task: 
      - name: practiceing Standard loops in ansible
        apt:
          name: "{{ items }}"
          state: present
    loop: 
      - git
      - tree
      - apache2