---
  - hosts: 172.31.2.29
    become: yes
    tasks:
      - name: installing java on ubuntu node
        apt:
         name: "{{package_3}}"
         state: present
        when: ansible_os_family =="Debian"
    
